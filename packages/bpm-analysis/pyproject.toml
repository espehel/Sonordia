[project]
name = "bpm-analysis"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
    "essentia",
    "numpy",
    "mutagen",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bpm_analysis"]
